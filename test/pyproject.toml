[project]
name = "test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "gespel", email = "heimbrodt@uni-potsdam.de" }
]
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.1",
    "psycopg[binary]>=3.3.4",
]

[project.scripts]
test = "test:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
